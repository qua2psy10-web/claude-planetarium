export interface HygStar {
  hip: number;
  proper: string;
  ra: number;   // radians
  dec: number;  // radians
  mag: number;
  ci: number;   // B-V color index
  con: string;  // constellation abbreviation
}
